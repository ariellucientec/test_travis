matrix:
  include:
    - language: python
      python: 3.7
      script:
        - pip install sympy
    - language: cpp
      compiler: g++
      script: 
        - g++ -std=c++17 main.cpp
        - ./a.out

